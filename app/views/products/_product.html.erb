
  <div class="col-md-3 col-xs-6">
    <%= link_to image_tag(product.image_url, class: "img-responsive p_img"), product_path(product) %>
    <p class="p_name"><%= product.name %></p>
    <p class="p_description"><%= product.description %></p>
    <p class="p_price">€ <%= product.price %></p>
    <% if signed_in? && current_user.admin? %>
      <p><%= link_to 'Edit', edit_product_path(product) %> |
      <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <% end %>
  </div>
